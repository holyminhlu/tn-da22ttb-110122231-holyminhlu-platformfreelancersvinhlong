export default function IdentityReadOnlyBanner() {
  return (
    <p className="idv-readonly-banner" role="status">
      Hồ sơ đã được duyệt — bạn chỉ có thể xem lại thông tin, không thể chỉnh sửa hoặc nộp lại.
    </p>
  );
}
